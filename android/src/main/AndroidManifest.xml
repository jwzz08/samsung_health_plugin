<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.samsung_health_plugin">

    <!-- Samsung Health SDK 권한 -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.BODY_SENSORS_BACKGROUND" />
    <uses-permission android:name="com.samsung.health.permission.read" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Samsung Health 앱 접근 권한 -->
    <queries>
        <package android:name="com.samsung.android.health" />
    </queries>

    <application>
        <!-- Samsung Health SDK 관련 설정 -->
        <meta-data
            android:name="com.samsung.android.health.permission.read"
            android:value="com.samsung.health.data_type.read" />
    </application>
</manifest>